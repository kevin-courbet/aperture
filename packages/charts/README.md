# Aperture

Aperture provides typed, accurate, and accessible React charts on TanStack
Charts. Import components from `@kevin-courbet/aperture`. The default package
entry loads the Aperture theme and IBM Plex Sans assets.

Use `@kevin-courbet/aperture/tanstack` only when the common API does not meet
an advanced chart requirement.
